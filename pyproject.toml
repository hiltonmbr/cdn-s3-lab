[project]
name = "cdn-s3-lab"
version = "1.0.0"
description = "Hands-on Object Storage Lab with RustFS, Erasure Coding, and Data Lakehouse"
requires-python = ">=3.12"
dependencies = [
    "jupyterlab>=4.1",
    "boto3>=1.34",
    "pandas>=2.2",
    "pyarrow>=15.0",
]

[tool.uv]
